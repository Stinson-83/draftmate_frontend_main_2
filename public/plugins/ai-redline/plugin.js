(function(window, undefined){
    window.Asc.plugin.init = function() {
        // Handled by sidebar interface
    };

    window.Asc.plugin.button = function(id) {
        this.executeCommand("close", "");
    };
})(window, undefined);
