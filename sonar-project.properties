# Required metadata
# Replace these with your actual keys from SonarCloud if they differ.
sonar.projectKey=Preetkakdiya_draftmate_frontend_main_2
sonar.organization=preetkakdiya

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Comma-separated paths to directories with sources
sonar.sources=.

# Exclude dependencies and compiled output from being scanned
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/coverage/**,**/*.spec.js,**/*.test.js,**/draftmate-chart/**

# Language settings (SonarCloud automatically detects, but you can force it)
# sonar.language=js,py
